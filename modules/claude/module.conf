# Source directory (expanded at runtime)
SOURCE_DIR="$HOME/.claude"

# Files and directories to sync (relative to SOURCE_DIR)
SYNC_ITEMS=(
    "CLAUDE.md"
    "SOUL.md"
    ".mcp.json"
    "settings.json"
    "keybindings.json"
    "scripts/"
    "skills/"
    "stacks/"
)
