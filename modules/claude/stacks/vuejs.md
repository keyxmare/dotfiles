# Stack Vue.js

## Vue 3
- Vue 3 avec la Composition API (`<script setup>`).
- Préférer `ref()` et `computed()` plutôt que l'Options API.
- TypeScript encouragé dans les composants Vue.
- Utiliser les composables pour la logique réutilisable.
- Pinia pour le state management si nécessaire.
